# v0.4.65

From v0.4.64, pull and restart. No database schema change.

## API contract documentation

- Document the structured producer source form `dify/<module>/<execution-version>` (for example, `dify/1-6-4投资决策/V3.15.6`) in the public OpenAPI contract.
- Explain that the Portal recognizes the trailing `V...` suffix and displays it as the report execution version.
- Keep the `version` component of the report identity key explicit in the OpenAPI contract and add a regression test for the source and query-filter descriptions.

The matching Dify API-provider descriptions and the checked-in Dify tool schemas were synchronized with this contract.
