# v2026.38.6

## Version dialog

- Treat a click on the footer version as ordinary release-history browsing, without showing the unsaved-work warning from the site's update-reminder policy.
- Keep long pages at their current reading position when release notes open by avoiding the document-level scroll lock that collapsed the page to one viewport.

No database schema changes are required.
