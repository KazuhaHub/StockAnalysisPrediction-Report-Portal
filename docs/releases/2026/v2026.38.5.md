# v2026.38.5

## Update delivery

- Add an automatic update policy that refreshes open pages when a new build is deployed and confirms the loaded version afterward.
- Fall back to a closable update dialog if the refreshed page still serves the old build, without entering a reload loop.
- Turn a closed update dialog into a persistent site-wide banner so readers can continue working without losing the reminder.
- Give older browser bundles a persistent banner during the first deployment of the automatic policy.

## Release history

- Let readers browse the 10 most recent packaged release notes directly from the version dialog, including a compact mobile selector.
- Package recent notes into each release binary so history remains available without GitHub access or credentials.
- Keep update dialogs pinned to their target version while allowing the ordinary version dialog to browse history.

## Version dialog

- Preserve the current scroll position when opening release notes from the version label, avoiding the apparent jump to the top of the page.

No database schema changes are required.
